# Swarm Skill

## Purpose
Coordinates multiple specialized agents to solve complex, parallelizable tasks. USE WHEN a task can be broken into independent sub-tasks or requires multiple perspectives (e.g., Code + Security).

## Components
- `SKILL.md`: Main instructions and triggers.
- `tools/`: CLI executable scripts.
